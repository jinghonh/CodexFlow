# CodexFlow 领域术语

CodexFlow 重建项目中的 Codex 工作过程，帮助用户理解不同会话如何共同形成项目结果，以及跨项目会话在主题上的联系。

## 项目与分组

**项目（Project）**：用户希望一起回顾的一组相关 Codex 工作，是会话、工作流和项目内关系的共同归属范围。同一 Git 仓库的主工作区和 worktree 属于同一项目；相同远程地址的独立克隆不因此成为同一项目。

**工作流（Workstream）**：围绕同一具体工作目标形成的会话分组，用于描述一条可追溯的推进过程。工作流依据已判断的具体工作关系形成，一条会话至多属于一个主要工作流。跨工作流联系继续作为关系展示。

**主题（Topic）**：描述会话主要讨论内容的全局分类，与项目归属和工作流成员关系分开。主题组可以跨项目；一条会话至多有一个主要主题，置信度不足时保持未分类。

**未分组会话**：尚未归入主要工作流的会话；它仍然属于项目并可在项目视图中查看。

**未分类会话**：没有达到主题归属门槛的会话；它仍然属于项目并可在项目视图中查看。

**会话（Thread）**：由 Codex Thread ID 标识、可以恢复并继续进行的独立工作记录，是 CodexFlow 的基本分析单位。
_避免_：将一次恢复、单个回合或整个工作流称为一条新会话。

**回合（Turn）**：一条会话中一次用户输入及其后续代理执行对应的记录，是活动时间与来源定位的组成单位。

**条目（Item）**：回合中的一项来源记录，例如消息、命令执行或文件修改。

## 关系与判断

**观察关系（Observed）**：来源数据明确记录的会话关系，例如派生会话或子代理关系。

**候选会话对（Candidate Pair）**：由明确引用、共享产物、分支、时间接近、词语重合或语义相似等信号召回的一对会话。候选只表示值得判断，不构成关系边，也不直接决定工作流归属。

**推断关系（Inferred）**：由 Jev 对候选会话对判断出的有类型关系，包含方向、置信度、执行阶段、结果和判断说明。只有支持的判断可作为关系边；未知或不支持不成边。

**执行阶段（Execution Stage）**：具体关系所处的推进程度，分为计划中、已开始和已完成。执行阶段影响关系对工作流分组的强度。

**执行结果（Execution Outcome）**：执行阶段的结果，如成功、受阻或失败。结果与执行阶段分开记录，不改变关系边的分组权重。

**关系判断说明（Jev Rationale）**：由 Jev 的类型化关系、方向、执行阶段和结果组合成的可读说明。它呈现关系判断，不等同于自由文本推理或来源证据。

**证据（Evidence）**：支持事实或判断的来源记录及其定位信息；解释文本本身不是来源证据。

**用户裁决**：用户对推断关系作出的确认或拒绝。确认表示用户接受该判断，不改变关系的推断来源。

## 时间与分析

**活动区间**：有来源时间依据的一段回合活动；一条会话可以包含多个分离的活动区间。

**已知活动时长**：来源记录中可用回合时长的累计值；数据不完整时不代表会话的全部活动时长。

**分析运行（Analysis Run）**：对选定项目数据执行的一次模型分析过程，包含会话总结、候选召回、关系判断、主题归属及工作流命名等步骤。
