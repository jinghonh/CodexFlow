{
  "name": "codexflow",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "build:macos:local": "npm run tauri -- build && bash scripts/sign_macos_local.sh",
    "test:ui": "vitest run",
    "tauri": "env -u AR -u CC -u CXX -u CPP -u LD -u NM -u CFLAGS -u CXXFLAGS -u CPPFLAGS -u LDFLAGS tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0",
    "@tauri-apps/plugin-dialog": "^2.0.0",
    "@xyflow/react": "^12.11.6",
    "elkjs": "^0.12.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "@testing-library/react": "^16.3.3",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.0.0",
    "jsdom": "^30.1.1",
    "typescript": "^5.0.0",
    "vite": "^6.0.0",
    "vitest": "^4.1.11"
  }
}
