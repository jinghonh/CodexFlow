[package]
name = "codexflow-store"
version.workspace = true
edition.workspace = true

[dependencies]
codexflow-domain = { path = "../domain" }
rusqlite = { version = "0.37", features = ["bundled"] }
serde = "1"
serde_json = "1"
