[package]
name = "codexflow-core"
version.workspace = true
edition.workspace = true

[dependencies]
codexflow-domain = { path = "../domain" }
codexflow-codex = { path = "../codex" }
codexflow-store = { path = "../store" }
codexflow-jev = { path = "../jev" }
tokio = { version = "1", features = ["sync", "macros", "rt"] }
