# Graph overlay 采用提交式保存和显式冲突处理

Graph overlay 在用户操作提交时保存，刷新遇到未保存修改必须由用户选择保存、丢弃或取消；发现外部文件变化时不得静默覆盖，而应展示差异并提供重新加载、保存副本或明确覆盖。写入使用原子替换并结合文件锁或版本/哈希检查，旧版本先在内存中迁移并备份原文件；重复 YAML 键视为解析错误，未知字段尽量保留，更高版本按只读不兼容处理。这样优先保护人工关系和布局不被覆盖，代价是保存流程需要显示 dirty、冲突和迁移状态。
