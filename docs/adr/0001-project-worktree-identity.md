# 同一仓库的 worktree 共享项目身份

CodexFlow 以项目整体的工作过程作为回顾对象，因此同一 Git 仓库的主工作区和 worktree 合并为一个项目，同时保留每条会话的工作区来源。相同远程地址的独立克隆不自动合并，避免将独立工作副本中的历史仅凭远程地址混为一组。

此决定由用户于 2026-09-23 确认，替代旧 MVP 按每个工作区根分别建立项目的约定。项目解析、关系候选、工作流与项目级验收均须遵守此边界。
