# 本地运行时仅暴露回环地址

生产环境采用一个本地应用，由后端管理 Codex app-server 并提供已构建的前端；服务只监听 127.0.0.1，Project 路径由本地选择或启动参数确定，并在真实路径校验后限制文件访问范围。开发时可以单独运行 Vite；数据源不可用必须显示为来源错误，不能伪装成 missing。这个边界优先限制任意文件读取和局域网暴露，代价是后端需要管理前端和 app-server 的生命周期。
