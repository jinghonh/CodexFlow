# V1-19 性能实施与测量记录

## 数据与环境

- 数据集版本：`issue-30-v1`。生成器固定产生单项目 500 条会话、每条 20 回合、每回合 10 条目，总计 1 万回合与 10 万条目。499 条观察关系与 2500 条规则关系构成 500 节点、2999 条可见关系的图。生成器在独立临时目录初始化正式数据库结构，并直接写入合成来源记录；不会读取或复制用户历史。
- 基准机器：`Mac16,12`，Apple M4，16 GiB 内存，macOS 27.0（26A428），`arm64`。
- 基线：`3f505e9722cc83b8e3b1e066021d18c3ef175f9c`。当前性能分支的最终提交见本文件所在提交。
- 桌面构建：Tauri 正式发布配置，应用版本 `0.1.0`。为隔离用户现有缓存，测量包仅覆盖标识与名称：`dev.codexflow.performance`、`CodexFlow Performance`；应用代码与资源仍由 `tauri build` 发布构建产生。`Contents/MacOS/codexflow-desktop` 的 SHA-256 为 `7e35001d6c98fa6cd8e98694af342a032582ae71304f85dfa1f45411e7fb96ca`。构建命令见下文。桌面实际交互尚未完成，不能把核心与独立布局测量冒充为桌面端到端指标。
- 20 次缓存测量时没有其他 `cargo`、`rustc` 或 CodexFlow 测量进程并发运行。测量期间未运行真实模型调用。原始逐次数据保存在 [核心结果](results/issue-30-core.txt)与[布局结果](results/issue-30-layout.txt)。

## 复现

以下命令中的目录必须是全新、可丢弃的路径。

```sh
npm ci
cargo run --release -q -p codexflow-core --example performance -- generate /tmp/codexflow-perf-issue30-v1
cargo run --release -q -p codexflow-core --example performance -- measure /tmp/codexflow-perf-issue30-v1 20
cargo run --release -q -p codexflow-core --example performance -- export-graph /tmp/codexflow-perf-issue30-v1 > /tmp/codexflow-perf-graph.json
node scripts/measure_graph_layout.mjs /tmp/codexflow-perf-graph.json 20
npm run tauri -- build --config '{"identifier":"dev.codexflow.performance","productName":"CodexFlow Performance"}'
```

测量程序沿公开核心接口调用项目目录、项目会话、时间线、关系图、工作流、候选、会话查询与过滤，以及回合、条目、事实首屏分页。单次查询最多读取对应分页；它核对数据库三张主表的行数与图可见关系数。布局脚本对公开关系图结果使用桌面端相同的 ELK 参数及结构骨架选择，计时仅覆盖 ELK 布局，未包含 Worker 加载、React Flow 或 WebView 绘制。

## 结果

| 核心操作 | 20 次中位数 | 第 95 百分位 | 最大值 |
| --- | ---: | ---: | ---: |
| 进程内核心服务首次启动 | 28.7 毫秒（单次） | — | — |
| 项目目录 | 0.337 毫秒 | 0.412 毫秒 | 0.566 毫秒 |
| 项目会话 | 0.901 毫秒 | 1.089 毫秒 | 1.183 毫秒 |
| 项目时间线 | 11.382 毫秒 | 20.834 毫秒 | 76.196 毫秒 |
| 关系图查询 | 341.017 毫秒 | 388.617 毫秒 | 388.698 毫秒 |
| 工作流查询（含关系图） | 657.147 毫秒 | 681.530 毫秒 | 682.014 毫秒 |
| 候选预览 | 333.329 毫秒 | 371.774 毫秒 | 418.067 毫秒 |
| 会话文字查询 | 46.676 毫秒 | 52.416 毫秒 | 56.604 毫秒 |
| 会话归档过滤 | 46.166 毫秒 | 51.968 毫秒 | 54.226 毫秒 |
| 证据回合首屏 | 0.242 毫秒 | 0.373 毫秒 | 0.627 毫秒 |
| 证据条目首屏 | 0.224 毫秒 | 0.264 毫秒 | 0.483 毫秒 |
| 结构化事实首屏 | 0.590 毫秒 | 0.750 毫秒 | 1.067 毫秒 |
| 500 节点布局，2999 条边全部保留绘制 | 48.196 毫秒 | 92.114 毫秒 | 124.744 毫秒 |

首次冷服务调用见[核心结果](results/issue-30-core.txt)的 `run=0`：时间线 76.196 毫秒、关系图 388.617 毫秒、工作流 678.466 毫秒。服务启动计时只包含 `SourceService::new`，不包含桌面应用进程或 WebView 启动。首次候选构建和事实提取需要另行计时；上述缓存数据不用于推断该路径的延迟。

原实现的 [20 次基线](results/issue-30-baseline.txt)显示服务启动 1744.978 毫秒、关系图查询第 95 百分位 2044.721 毫秒。对同一 2999 边图直接运行全边 ELK 分层布局，前两次分别耗时约 24.9 秒和 27.4 秒；确认远超 5 秒后终止该未完成的 20 次运行。结构骨架布局的 20 次完整结果见上表。所有 2999 条关系仍传给 React Flow；499 条仅用于计算节点位置。

## 修复与验证范围

- 项目归属核对在一次重算内复用相同工作目录的规范路径和 Git 解析结果，避免 500 条会话逐条启动 Git 子进程。
- 候选与规则关系在输入版本未变化时复用数据库缓存；输入版本包含项目会话、来源内容修订、事实规则与候选规则。失效或存在旧候选时重新生成并保留既有正确性检查。
- 关系图在 Worker 中执行 ELK。分层布局仅以来源优先的生成森林确定位置，仍将全部关系传入图组件；快速切换过滤时取消旧 Worker。筛选后的边在重新布局前立即更新。
- 桌面桥接将会话查询、关系图、工作流、候选预览与时间线查询交给阻塞任务池执行，避免这些同步数据库和聚类工作占用 Tauri 的事件处理线程。
- 时间线初始展示 40 条会话，可继续分页浏览；具体回合、条目和事实仍由现有公开接口分页读取，不把 10 万条目装进 UI。
- `cargo test -p codexflow-core -p codexflow-store`、相关时间线与关系图 UI 测试、`npm run build` 均通过。缓存命中与失效、关系保留及时间线分页有回归断言。

## 待完成的桌面验收

正式桌面应用已构建，基准应用数据与用户原有应用标识隔离。此机器在执行桌面操作时处于锁屏状态，计算机操作接口返回“Mac is locked and automatic unlock could not unlock it”。因此尚未测量应用首次打开至概览可交互、WebView 中的 500 节点与 2999 边完整绘制、后台索引或分析时的交互与取消。上述门槛不能仅凭核心与 ELK 独立结果宣称通过。解锁后应在空闲机器上用正式包沿真实桌面操作路径完成至少 20 次缓存测量和一次完整冷启动记录。

真实 Codex 来源读取与 Jev 模型服务未用于固定输入测试，也没有被计入本地响应指标；其延迟、进度和失败率需在具备受控来源与服务配置时另报。超过本数据集规模没有写入硬上限，性能范围尚未测量。
